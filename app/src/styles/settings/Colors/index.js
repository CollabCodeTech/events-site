import { createGlobalStyle } from "styled-components";

const Colors = createGlobalStyle`
  :root {
    --color-negative: #0e1d32;
    --color-negative-dark: #09121e;

    --color-zero: #FFF;
  }
`;

export default Colors;
